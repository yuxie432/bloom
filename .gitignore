# dependencies (not needed to run this app)
node_modules/

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# local backups exported from the app
pourover-backup-*.json

# converted data + report (import via the app, don't commit)
bloom-import.json
conversion-report.md
convert.py

# agent handoff notes (local only)
HANDOFF.md
